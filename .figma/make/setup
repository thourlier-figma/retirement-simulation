#!/bin/sh
set -e

# Install Node.js if not present
if ! command -v node &> /dev/null; then
  if ! command -v brew &> /dev/null; then
    echo "Error: Homebrew is required to install Node.js. Install it from https://brew.sh"
    exit 1
  fi
  brew install node
fi
